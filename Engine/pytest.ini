[pytest]
testpaths = app/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
markers =
    benchmark: marks tests that require the benchmark.wav fixture (deselect with '-m "not benchmark"')
